{% comment %}

  Loop through a defined number of articles with the 'paginate' tag.
  Don't forget to close the tag after your loop.

{% endcomment %}

{% paginate blog.articles by 5 %}

{% comment %}
  This template is also used for listing out articles of a specific tag.
  You can change layouts or styles by checking if current_tags is true.
{% endcomment %}


{% comment %} Two column layout for featured blog posts, other posts and the social feeds. {% endcomment %}
<div class="grid-item remove_padding large--three-quarters push--large--one-eighth medium--one-whole">

  {% comment %} Header may not be needed, just comment it out if it's not needed {% endcomment %}
    <header class="page-header text-center large--five-sixths push--large--one-twelfth medium--five-sixths push--medium--one-twelfth">

      {% if current_tags %}
        <h1>{{ blog.title | link_to: blog.url }} &raquo; {{ current_tags.first }}</h1>
      {% else %}
        {% comment %} Change title of page to 'The Closet Girl'
        <h1>{{ blog.title }}</h1>
        {% endcomment %}
        <h1>The Closet Girl</h1>
      {% endif %}

    </header>
    
  {% comment %} COLUMN 1 {% endcomment %}
  
  <div class="grid-item large--three-fifths medium--three-fifths remove_padding">  

    {% comment %} 
      Content for featured blog posts go here 
    <div class="flexslider">
      <ul class="slides">
        <li>
          {{ '600x360-placeholder.png' | asset_url | img_tag }}
        </li>
        <li>
          {{ '600x360-placeholder.png' | asset_url | img_tag }}
        </li>
        <li>
          {{ '600x360-placeholder.png' | asset_url | img_tag }}
        </li>
      </ul>
    </div>
    {% endcomment %}

    

    {% comment %}
      Loop through each article in your blog. This is limited to what you set for pagination above.

      For more info on article liquid tags:
        - http://docs.shopify.com/themes/liquid-variables/article
    {% endcomment %}

    {% for article in blog.articles %}
    <div class="grid-item large--one-whole">
      <h3><a href="{{ article.url }}">{{ article.title }}</a></h3>
      <h6>
        Posted by {{ article.author }} 
        {% comment %} at
        <time pubdate datetime="{{ article.published_at | date: '%Y-%m-%d' }}">
          {{ article.published_at | date: '%b %d, %Y' }}
        </time>
        {% endcomment %}
      </h6>

      {% comment %}
        Add a surrounding div with class 'rte' to anything that will come from the rich text editor.
        Since this is just a listing page, you can either use the excerpt or truncate the full article.
      {% endcomment %}
      <div class="rte">
        {% if article.excerpt.size > 0 %}
          <a href="{{ article.url }}">{{ article.excerpt }}</a>
        {% else %}
          <p>{{ article.content | strip_html | truncatewords: 100 }}</p>
        {% endif %}
      </div>

      {% comment %}
        Show off meta information like number of comments and tags.
      {% endcomment %}
      <ul>

        {% comment %}
          Don't be fooled, the ? in comments_enabled? is part of the liquid tag.
        {% endcomment %}
        {% if blog.comments_enabled? %}
        <li>
          <a href="{{ article.url }}#comments">
            {{ article.comments_count }} {{ article.comments_count | pluralize: 'comment','comments' }}
          </a>
        </li>
        {% endif %}
        {% comment %}
          {% include 'tags-article' %}
        {% endcomment %}
      </ul>

      <p><a class="blog-read-more" href="{{ article.url }}">Read more</a></p>

      <hr class="large--five-sixths push--large--one-twelfth medium--five-sixths push--medium--one-twelfth">
    </div>
    {% endfor %}

    {% if paginate.pages > 1 %}
    <div class="text-center">
      {% include 'pagination-custom' %}
    </div>
    {% endif %}

  </div> {% comment %} END OF COLUMN 1 {% endcomment %}


  {% comment %} COLUMN 2 {% endcomment %}
  <div class="grid-item large--two-fifths medium--two-fifths remove_padding">

    <div class="grid-item medium--one-whole">
      <div id="contentplum_widget_836344032"></div>
      <script type="text/javascript">new contentplum_widget_function('contentplum_widget_836344032','0','1');</script>
    </div>
  </div>
  {% comment %} END OF COLUMN 2 {% endcomment %}


</div>

{% endpaginate %}